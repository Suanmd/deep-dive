## Summary

One or two sentences explaining the change.

## Motivation

Why is this change needed? Link the issue it closes (if any).

## Changes

- What changed
- What was added
- What was removed

## Testing

- [ ] Unit tests added/updated
- [ ] Integration tests added/updated (if applicable)
- [ ] `pytest` passes locally
- [ ] `ruff check src tests` clean
- [ ] `ruff format --check src tests` clean
- [ ] `mypy src/deep_dive` clean

## Configuration

If you changed `config/defaults.yaml`, list the new keys and their defaults.

## Checklist

- [ ] CHANGELOG.md updated (under "Unreleased")
- [ ] Docs updated (if public API / CLI flags changed)
- [ ] No secrets / cookies / local paths committed
